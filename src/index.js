import React from 'react';
import ReactDOM from 'react-dom/client';
import FormVal from './FormVal';
import FormVal2 from './FormVal2';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    {/* <FormVal/> */}
    <FormVal2/>
  </React.StrictMode>
);


